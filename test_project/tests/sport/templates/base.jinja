<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
{#    <link rel="icon" href="../../favicon.ico">#}

    <title>{{ title }}</title>
    <link href="{{ 'libs/css/bootstrap.min.css'|static }}" rel="stylesheet">
{#    <link href="{{ 'css/crud.css'|static }}" rel="stylesheet">#}
  </head>

  <body>
    <nav class="navbar navbar-default navbar-static-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="/">Django CRUD Demo</a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            <li {% if 'town' in request.path %}class="active"{% endif %}>
              <a href="{{ url('town-list') }}">Towns</a>
            </li>
            <li {% if 'team' in request.path %}class="active"{% endif %}>
              <a href="{{ url('team-list') }}">Teams</a>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <div class="container">
      {% block container %}{% endblock %}
    </div>

  {% block js %}
  <script src="{{ 'libs/js/jquery.min.js'|static }}"></script>
  <script src="{{ 'libs/js/bootstrap.min.js'|static }}"></script>
  {% endblock %}
  </body>
</html>
