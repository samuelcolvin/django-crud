{% extends base_template %}

{% block container %}
  {% if prefix_include %}
    {% include prefix_include %}
  {% endif %}
  <div class="row">
    <div class="col-md-{% if search_form %}9{% else %}12{% endif %}">
      {% if object_list %}
        <table class="table">
          <thead>
          <tr>
            {% for column, name, value, css_classes in view.gen_object_short_with_column(object_list[0]) %}
              <th class="{{ css_classes }}">{{ name }}</th>
            {% else %}
              <th>{{ view.model._meta.verbose_name }}</th>
            {% endfor %}
          </tr>
          </thead>
          <tbody>
          {% for object in object_list %}
            <tr>
              {% for  column, name, value, css_classes in view.gen_object_short_with_column(object) %}
                <td class="{{ css_classes }}">
                  {{ value }}
                </td>
              {% else %}
                <td>
                  <a href="{{ view.get_detail_url(object) }}">{{ object }}</a>
                </td>
              {% endfor %}
            </tr>
          {% endfor %}
          </tbody>
        </table>
        {{ display.pagination(page_obj, get_without_page) }}
      {% else %}
        <h3>{% trans %}No {{ plural_model_name }} found{% endtrans %}</h3>
      {% endif %}
    </div>
  </div>
{% endblock %}
